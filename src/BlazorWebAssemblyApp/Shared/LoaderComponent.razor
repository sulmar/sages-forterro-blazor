@typeparam TItem    

@if (Items == null)
{
    <TitleComponent />
}
else
{
    @ChildContent
}

@code {

    [Parameter]
    public IEnumerable<TItem> Items { get; set; }

    [Parameter, EditorRequired]
    public required RenderFragment ChildContent { get; set; } // Nazwa ma znaczenie!

}
